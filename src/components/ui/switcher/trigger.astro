---
type Props = {
    class?: string;
};

const { class: className } = Astro.props;
---

<button
    class:list={[
        className ? className : "px-4 py-2",
        "w-full rounded-md bg-neutral-200 text-neutral-800 data-open:rounded-b-none",
    ]}
>
    <slot />
</button>
