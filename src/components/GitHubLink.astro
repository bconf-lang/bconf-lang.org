---
import { GithubIcon, type IconProps } from "@components/ui/icons";

type Props = {
    size?: IconProps["size"];
};

const { size = 48 } = Astro.props;
---

<a href="https://github.com/bconf-lang/bconf" aria-label="bconf GitHub">
    <GithubIcon size={size} />
</a>
